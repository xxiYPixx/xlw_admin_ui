import {
    UserFilled,
    Goods,
    Tickets,
    Coin,
    List,
    Management
} from '@element-plus/icons-vue'

export const iconMap = {
    UserFilled,
    Goods,
    Tickets,
    Coin,
    List,
    Management
}

export type IconName = keyof typeof iconMap
